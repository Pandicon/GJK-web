# `load_old_posts_json.py`

```sh
load_old_posts_json.py <file> [options]
```

option | effect
--- | ---
`--clear` | clears the article database before filling it

Loads specified json file with old posts and stores it into the article database.

